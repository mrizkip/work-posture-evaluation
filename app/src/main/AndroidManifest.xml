<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mrizk.workpostureevaluationrula_reba">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AboutActivity" />
        <activity android:name=".IntroductionActivity" />
        <activity android:name=".RulaActivity" />
        <activity android:name=".RebaActivity" />
        <activity android:name=".SudutActivity" />
        <activity android:name=".RulaAddtionalPartAActivity" />
        <activity android:name=".RulaAdditionalPartBActivity" />
        <activity android:name=".RebaAdditionalPartAActivity" />
        <activity android:name=".RebaAdditionalPartBActivity"></activity>
    </application>

</manifest>